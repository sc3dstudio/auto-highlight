# Blender
*.blend1
*.blend2
*.blend[0-9]
*_autosave.blend
*.crash.txt

# Renders / scratch output
*.png
*.exr
_probe/

# ... except the images the README and the submission need. Both patterns are
# needed: "!assets/*.png" does not reach into a subdirectory, so the platform
# images under assets/platform/ stayed ignored until this line existed.
!assets/*.png
!assets/**/*.png

# Build output
dist/

# Python
__pycache__/
*.py[cod]
.venv/
